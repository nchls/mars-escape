import React from 'react';
import { connect } from 'react-redux';

import './buildDialog.scss';


const BuildDialog = ({ }) => {
	return (
		<p>BuildDialog</p>
	);
};

const mapStateToProps = (state) => {
	return {

	};
};

export default connect(mapStateToProps)(BuildDialog);
